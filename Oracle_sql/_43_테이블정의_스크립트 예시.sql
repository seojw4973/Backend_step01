--SYSTEM 계정으로 처리하는 부분 START
-- 현재 bituser계정이 있으면 삭제하라
-- 만약 없으면 Error 발생, 무시해도 된다.


-- bituser를 id로 123456를 password로 정한다.
-- 기본 권한을 부여한다.


-- 권한을 부여하다. 연결, 리소스관리 

-- 뷰생성 권한, SYNONYM권한을 부여한다.


-- bituser계정으로 연결하기 전에 현재 계정의 Lock을 해제한다.




--bituser계정으로 실행한다.
--client 테이블을 삭제하라


--client 테이블을 생성하라


--server 테이블을 삭제하라


--server 테이블을 생성하라




